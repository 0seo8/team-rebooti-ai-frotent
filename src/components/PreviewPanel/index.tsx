import PDFPreview from '@/components/PDFPreview';
import * as S from './styles';

const PreviewPanel = () => {
  return (
    <S.Container>
      <PDFPreview />
    </S.Container>
  );
};

export default PreviewPanel;
