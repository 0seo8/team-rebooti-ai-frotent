import PDFCanvas from '@/components/PDFCanvas';
import * as S from './styles';

const EditorCanvas = () => {
  return (
    <S.Container>
      <PDFCanvas />
    </S.Container>
  );
};

export default EditorCanvas;
